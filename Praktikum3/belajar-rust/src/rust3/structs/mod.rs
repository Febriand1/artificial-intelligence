pub struct Person {
    pub name: String,
    pub age: u8,
}

pub struct Rectangle {
    pub width: u32,
    pub height: u32,
}

pub struct Counter {
    pub value: i32,
}

pub struct User {
    pub username: String,
    pub email: Option<String>,
}

pub struct Temperature {
    pub celsius: f64,
}

pub struct Email {
    pub address: String,
}

pub struct Circle {
    pub radius: f64,
}

pub struct Text {
    pub content: String,
}

pub struct Numbers {
    pub values: Vec<i32>,
}

pub struct Point {
    pub x: i32,
    pub y: i32,
}